<h1>New setting</h1>

<%= error_messages_for :setting %>

<% form_for(@setting) do |f| %>
  <%= render :partial => "form"
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', settings_path %>
